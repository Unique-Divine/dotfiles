# A tap is an extra package source. brew lines are packages.
# Core formulas like lychee need no tap. buf is not in core, so
# tap the bufbuild/buf repo first, then install from it.
tap "bufbuild/buf"
brew "bufbuild/buf/buf"
brew "kubectl"
brew "lychee"
brew "zig@0.15"
